---
name: Reminder — repush .exe to website
description: leaq asked to be reminded next session to push a new tag, get the GitHub Actions .exe, and republish the site with the new installer.
---

**Reminder for leaq (set 2026-06-17):**

Next session, remind him to:
1. Run the push command to trigger a new GitHub Actions build: `git tag v3.1.3 && git push origin v3.1.3` (or next available tag)
2. Wait for the Actions build to finish on GitHub → download the new `.exe`
3. Send the `.exe` here and republish the site so the download link points to the new version
